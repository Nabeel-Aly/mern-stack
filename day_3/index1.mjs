export let name = "nabil";
export let age = 22;

let country = "nepal";  

export default country;

/*
export by name
  prefix by export
  we can have many export by name in a file
export by default
  we can have only one export by default in a file
*/
